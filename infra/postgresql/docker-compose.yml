services:
  postgres-user:
    image: postgres:latest
    container_name: postgres-user
    environment:
      POSTGRES_USER: user_service
      POSTGRES_PASSWORD: user_service
      POSTGRES_DB: user_service
    ports:
      - "5440:5432"
    volumes:
      - postgres_data_user:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user_service -d user_service"]
      interval: 10s
      timeout: 5s
      retries: 5

  # postgres-payment:
  #   image: postgres:latest
  #   container_name: postgres-payment
  #   environment:
  #     POSTGRES_USER: payment_service
  #     POSTGRES_PASSWORD: payment_service
  #     POSTGRES_DB: payment
  #   ports:
  #     - "5441:5432"
  #   volumes:
  #     - postgres_data_payment:/var/lib/postgresql
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U payment_service -d payment"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

volumes:
  postgres_data_user:
  # postgres_data_payment:
